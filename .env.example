# ─── WLED matrix (used by clawd.py / clawd_daemon.py) ─────────────────
# Required for the Clawd mascot animations.
WLED_URL=http://wled.local
WLED_WIDTH=16
WLED_HEIGHT=16
# Flip the sprite horizontally/vertically if your matrix is wired such
# that Clawd appears mirrored compared to the demo GIF. Leave blank
# (or set to false) if he already looks correct.
WLED_MIRROR_X=false
WLED_MIRROR_Y=false
# Idle animation preset:
#   "default"  — look around, glance down, occasional wink
#   "dvd"      — DVD-logo diagonal bounce, surprised on corner hits
IDLE_MODE_PRESET=default
# Idle color mode:
#   "default"      — static dim Anthropic orange
#   "rainbow"      — slow rainbow hue cycle (~30 s)
#   "colorchange"  — change color on each wall hit (best with dvd preset)
IDLE_MODE_COLOR=default
# Brightness (0–255) for idle and active (working/done/error/etc.) states.
IDLE_BRIGHTNESS=50
ACTIVE_BRIGHTNESS=140

# ─── Home Assistant (used by flash.sh / flash_stop.sh) ────────────────
# Optional — skip these lines if you don't want the smart-light flashes.
HA_URL=https://your-home-assistant-host
HA_TOKEN=your_long_lived_access_token_here
HA_ENTITY=light.your_light_entity_id
